---
---

Add the role-exit engine: leaving a role that supplied a large part of an identity -- the doubts, the alternatives, the turning point, and the ex-role -- carrying the twist that a role can be quit and being its ex cannot. First release, so the manifest version is the initial version and the changeset is empty.
