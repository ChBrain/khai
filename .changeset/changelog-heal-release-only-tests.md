---
---

Add tests for the CHANGELOG heal discriminator: the pending-changesets helper's semantics, the working-branch case that must not rewrite a published heading, and the release cases that must still heal. Tests only; no shipped content changes.
