---
---

Add the guile engine (packages/engines/guile): the deceptive pole of the design family (build #2 of the discovery-loop set), the object designed to mislead the user for the maker's benefit -- the dark pattern, where the lie is the intent and not a defect. A depth-1 engine: the deceit root over five facets after Gray's taxonomy -- sneaking (hiding what would be refused), obstruction (the wanted path made hard), nagging (repetition until the user yields), forcing (an unwanted act for a wanted one), misdirection (the rigged interface, Brignull). Warrant Brignull (the coinage), Gray et al. (the taxonomy), Mathur et al. (measurement). Wires the default law at instructions/Knowledge and one specific link at piece/Apparent. Bounds against usability (an honest defect vs a designed lie -- both share Apparent, split on intent), agency (a social program vs a transaction against the user), the persona-side deception engine (interpersonal false belief vs the object built to deceive), and captology (deceiving once vs conditioning over time). First release; source only, tests to follow as a separate dormant PR.
