---
---

Add the hydrology engine's tests: package conformance (content + manifest + compose), the manifest shape (a water root over four modes, wired on place at Shown), and compose() carrying the hydrology root above each mode. Tests only; no shipped content changes.
