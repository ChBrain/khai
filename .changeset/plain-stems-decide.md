---
---

Record the maintainer's ruling in `docs/ENGINE-GAPS.md`: a distinct stem beats a
`memberPolicy.homonyms` whitelist. The two blocked rows now name what to build
under (`disappearance` for the performance record thinning, `social-touch` for
interpersonal contact), `calamity` keeps its name outright, and the four other
passages that offered the whitelist as a choice no longer do. Documentation only.
