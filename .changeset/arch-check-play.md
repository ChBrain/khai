---
"@chbrain/khai-arch": patch
---

checks/check_play.mjs: the play's mechanical shape (frontmatter subset, H1, the six ENACTS chapters in order and none empty, the house bytes) in one file that imports nothing but node, runnable on a file with an exit code. The canon re-exports its chapter list from it and exports checkPlay, so the kit, the hook, CI and the playwright skill can run the same bytes.
