---
---

Record the fire-adjacent engine cluster in docs/ENGINE-GAPS.md so any agent can pick
it up: hearth (the kept flame as institution), detonation (the blast proper, at
doctrine altitude), incendiary (fire turned against someone -- maintainer's call),
celestial (the sky's bodies as a place's Shown), and metabolism (recommended as a
body member, not an engine) -- the five delegations combustion's Restrictions declare
as other engines' ground, each with warrant, boundary, member sketch, stem-collision
notes, and build mechanics (own engine lane, source-then-dormant-tests PRs, empty
first-release changesets). Also adds docs/ENGINE-GAPS.md to the guard's shared
branchScope paths, since its own contract -- "update Status as items land" -- means
engine PRs must be able to carry it; until now no lane owned it at all.
