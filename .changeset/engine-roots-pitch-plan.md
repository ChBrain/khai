---
"@chbrain/khai-arch": patch
---

WIRE: name Plan and Pitch among the engine root types. The kit and model already treat them as engine cargo (`engineMembers` accepts any canon type; `model.md`'s cast group lists both), so this brings the Wire chapter's enumeration in line with what the canon already permits — an engine may now root on a plan or a pitch.
