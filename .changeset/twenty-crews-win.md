---
---

Governance: whitelist the member stem `framing` under `memberPolicy.homonyms` in `khai-guard.config.json`. It is a same-word-different-science homonym (the precedent set by `gratitude`, `pride`, `envy`): `pitch-mode` owns `position_framing.md` (Klaff frame control -- the sales-pitch move of setting the context before the offer), while the incoming `framing` engine owns the cognitive framing effect (Kahneman/Entman/Goffman -- how the presentation of the same content shifts judgment). Two distinct phenomena sharing the word, so the stem is exempted rather than collapsed. No package change; empty changeset.
