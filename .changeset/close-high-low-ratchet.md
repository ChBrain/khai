---
---

Delete the dead `high` and `low` entries from `memberPolicy.homonyms` now all
three engines have moved, returning both stems to the gate's hard protection.
Config and planning artifact only; no package changes.
