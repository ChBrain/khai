---
---

Add the celestial engine tests: the canon conformance sweep (validateEnginePackage
with executeCompose), the manifest shape (a process root over six modes on
place/Shown), and the compose chains, following the standard content-package test
shape. Lands after the engine per the source/tests rule. Also rebuilds docs/SCIENCE.md
so the index carries the detonation and celestial Origin rows, closing the
fire-adjacent build: the science map is current for every engine in the inventory.
