---
---

Refresh the generated `docs/SCIENCE.md` out of band: reindex the science map so the recently added aesthetics engine appears in the concordance, and so the forward map reflects the merged deterministic scholar filter (`(attrib)`/`(ed)` qualifier rows now collate under their real authors). Docs-only; ships no package change.
