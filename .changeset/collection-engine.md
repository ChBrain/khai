---
---

Add the collection engine: a series of objects withdrawn from use and bounded by a criterion only its keeper holds -- the collector, the hunting, the completing, the dispersal -- carrying the twist that a collection is made of what is missing from it. A piece engine, so the members hang flat beneath the object. First release, so the manifest version is the initial version and the changeset is empty.
