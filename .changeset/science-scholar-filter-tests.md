---
---

Add unit coverage for the deterministic scholar filter in `surnames()` (qualifier stripping, the uppercase-initial invariant, the declared placeholder, and real-author collation). Dormant until the source change lands. Test-only; ships no package change.
