---
"@chbrain/khai-tests": patch
---

registry build: heal the CHANGELOG heading only during a release, discriminating on pending changesets, so a build in a working branch no longer rewrites a published heading to a version that never shipped.
