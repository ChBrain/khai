---
---

Add the ergonomics engine's tests (packages/engines/ergonomics/tests/ergonomics.test.mjs), the tests PR following the merged source per the source-and-tests-separate rule. Validates conformance (validateEnginePackage with executeCompose), the manifest (piece fit root over four facets, both wiring altitudes with the piece link at Load Bearing), and compose() (every leaf composes root-first carrying the ergonomics root; the root sits above each facet; unknown and missing leaves throw). Tests only; no package content or version change.
