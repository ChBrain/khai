---
"@chbrain/khai-tests": patch
---

Add the science index: `khai-tests science build|verify` inverts every engine's REFERENCES.md Origin table into a generated `docs/SCIENCE.md` (science → engine), collating each scholar across engines by surname. A build-drift gate holds the committed index to source, mirroring the registry gate.
