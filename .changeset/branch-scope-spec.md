---
---

Branch-scope spec: dormant tests, the `branchScope` lanes config, and
`docs/BRANCHING.md`. Test and docs only, no published behavior changes; the
`@chbrain/khai-guard` release rides the source PR's changeset.
