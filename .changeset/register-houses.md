---
"@chbrain/khai-plays": patch
---

Register two missing houses on the bill and re-render the README from the whole registry.

- **Life 2 Live** (`l2`): `registry/l2.json`, programme `@chbrain/khai-plays-l2`, `repo` overridden to `ChBrain/L2` (the house repository is `L2`, not `khai-plays-l2`).
- **Misfits** (`misfits`): `registry/misfits.json`, both `package` (`@chbrain/khai-misfits`) and `repo` (`ChBrain/khai-misfits`) overridden, since this house does not follow the `khai-plays-<slug>` naming.
