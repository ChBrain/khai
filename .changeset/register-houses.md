---
"@chbrain/khai-plays": patch
---

Register missing houses on the bill. Add `registry/l2.json` for the Life 2 Live house and re-render the README from the whole registry. The card's `repo` is overridden to `ChBrain/L2` (the house repository is `L2`, not `khai-plays-l2`); the programme stays `@chbrain/khai-plays-l2`.
