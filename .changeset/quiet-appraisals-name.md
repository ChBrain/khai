---
"@chbrain/khai-engine-emotion": minor
---

Name all five readings for what the engine says they are: `process_anger` →
`process_appraisal_anger`, and the same for `disgust`, `fear`, `joy` and
`sadness`. Frees those five stems for the engines that own the feelings. The
family word comes from each file's own Owner line — "the appraisal this feeling
is one reading of". Member files are API and renaming one is breaking, hence
minor; `anger` and `joy` are linked by the `comic` composite, whose links follow
on their own lane.
