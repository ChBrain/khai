---
"@chbrain/khai-methods": patch
---

License alignment: declare the package under the repo dual-license (content + code) instead of MIT, since it ships method definitions as content. Drop the per-method `license` frontmatter field — methods are ideas (not copyrightable); provenance lives in `invented_by` and `source`.
