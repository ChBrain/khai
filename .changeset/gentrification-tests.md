---
---

Add the gentrification engine's tests: package conformance (content + manifest + compose), the manifest shape (a re-entry root over four facets, wired on place at Withheld), and compose() carrying the gentrification root above each facet. Tests only; no shipped content changes.
