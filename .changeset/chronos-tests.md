---
---

Add the chronos engine's tests: package conformance (content + manifest + compose), the manifest shape (a passage root over four facets, wired on piece at Yearbook), and compose() carrying the chronos root above each facet. Tests only; no shipped content changes.
