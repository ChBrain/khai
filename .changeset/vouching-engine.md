---
---

Add the vouching engine: standing lent to somebody else's claim -- a request drawing on a list fixed years earlier, a text that cannot be negative, a stake nobody names, and a reading that correctly discounts all of it. First release, so the manifest version ships as-is.
