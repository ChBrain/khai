---
---

Whitelist `embarrassment` under `memberPolicy.homonyms`, so the forthcoming standalone `embarrassment` engine can carry its root member name without colliding with the `shame` engine's `process_embarrassment.md`. This is the same promotion pattern already cleared for the basic emotions and others: `shame` keeps embarrassment as a node at its lighter end (a relative of shame), while the standalone engine unfolds embarrassment in its own right -- the acute, self-conscious emotion of unwanted social exposure, its forms (faux pas, conspicuous, cringe, blush), and the trait embarrassability. Governance config only; ships nothing. The member-check ratchet's dead-exemption warning is expected until the `embarrassment` engine lands and makes the collision live.
