---
---

Add the meal engine: eating together as an instrument -- the table, the sharing, the order, and the bond -- carrying the twist that a meal binds in proportion to how little it is about binding. First release, so the manifest version is the initial version and the changeset is empty.
