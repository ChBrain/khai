---
---

Add the agency engine's tests (packages/engines/agency/tests/agency.test.mjs), the tests PR that follows the merged source per the source-and-tests-separate rule. Validates conformance (validateEnginePackage with executeCompose), the manifest (piece delegate root over four facets, both wiring altitudes with the piece link at Load Bearing, not usability's Apparent), and compose() (every leaf composes root-first carrying the agency root; the root sits above each facet; unknown and missing leaves throw). Tests only; no package content or version change.
