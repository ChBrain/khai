---
---

Add the advice engine: a second judgement placed next to a first -- the approach that sets what it will mean, the weighing that moves almost nothing, the return that tells the adviser otherwise, and the keeping, which is a second name on the decision. First release, so the manifest version ships as-is.
