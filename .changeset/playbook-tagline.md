---
"@chbrain/khai-arch": patch
---

Add `playbookTagline` export, sourced from a new `tagline` key in model.md's playbook block, so a consumer surface can print the canon's own description of the playbook verbatim instead of authoring its own copy.
