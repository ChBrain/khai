---
"@chbrain/khai-engine-gender": patch
---

Add the Playwright wiring guide to the gender engine. Gender is a position (the
read the room assigns before a persona speaks, not a trait it owns), system-wide
(every persona carries one). Anchors: persona/Projection (the carried read,
locked by the law) and persona/Shadow (a concealed different read, optional), the
gap between them the drama. Wired by linking the play to the engine.
