---
---

Add the drift engine's tests (packages/engines/drift/tests/drift.test.mjs), the tests PR that follows the merged source per the source-and-tests-separate rule. Validates conformance (validateEnginePackage with executeCompose), the manifest (process root over three mechanisms, both wiring altitudes including the first-of-kind process/Direction link), and compose() (every leaf composes root-first carrying the drift root; the root sits above each mechanism; unknown and missing leaves throw). Tests only; no package content or version change.
