---
---

Audit `docs/ENGINE-GAPS.md` against the tree: give every candidate table a
Status column and fill the 24 rows that had shipped and still read as unbuilt,
correct two rows whose stems are taken by shipped engines modelling something
else, record the 96 packages that landed with no row at all, fix the cross-type
map's type enumeration, refresh the inventory counts, and chart seven new
candidates found from the catalogue's own disclaimers. Documentation only.
