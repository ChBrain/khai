---
---

Add the Roadie design spec (`docs/ROADIE.md`): the deployment/composition model
-- the Roadie's two jobs (set up the Stage, take on Tour), Standards x Adaptions,
the two instruction seams (Knowledge for engines, System for deployment), the
three axes, the Venue + kind model, the engine's two wirings, and the build
order. Docs only; no shipped change.
