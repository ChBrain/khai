---
---

Gate this repository's own `docs/SCIENCE.md` against a fresh build. The drift
gate was fixture-only, so the index could go a whole engine stale without
anything noticing -- and did. Tests only; they do not ship.
