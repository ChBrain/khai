---
---

Chase the 49 live `memberPolicy.homonyms` entries for distinct names in
`docs/ENGINE-GAPS.md`. Every one has a rename; eighteen are the field's own
compound term rather than the bare word the whitelist was holding. Records the
inbound-link cost per rename, the build order it implies, and why this is a
backlog of ~40 engine-lane PRs rather than one. Documentation only.
