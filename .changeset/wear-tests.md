---
---

Add the wear engine's tests: package conformance (content + manifest + compose), the manifest shape (a use root over four modes, wired on piece at Load Bearing), and compose() carrying the wear root above each mode. Tests only; no shipped content changes.
