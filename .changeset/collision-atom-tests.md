---
---

Tests for the cross-kind display-title collision wall: a unit suite for the `titleCollisions` atom (cross-kind flagged, same-kind repetition and distinct titles not flagged, case-insensitive, basename and full-path exemptions, empty titles skipped) and an integration suite driving `validateProject` over a temp house (collision flagged, Playwright guide exempt, same-kind and distinct casts clean). The engine-member path and the meta-engine exemption are already covered by the conformance suite. Tests only; ships nothing.
