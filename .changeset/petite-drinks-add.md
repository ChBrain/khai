---
---

Add the `adhd` composite: ADHD read as the compound of a pause that does not
arrive, a delay that cannot be sat in, a default-mode drift that intrudes
mid-task, and an attention that will not release. First release — the manifest
version is the initial version, so this changeset is empty by rule 4.
