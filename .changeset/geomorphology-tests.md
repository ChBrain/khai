---
---

Add the geomorphology engine's tests: package conformance (content + manifest + compose), the manifest shape (a ground root over four modes, wired on place at Shown), and compose() carrying the geomorphology root above each mode. Tests only; no shipped content changes.
