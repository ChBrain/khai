---
"@chbrain/khai-engine-grief": minor
---

Rename the grief engine's `restoration` member to `rebuilding`. Same phenomenon (Stroebe & Schut's restoration orientation -- turning to rebuild the changed world), renamed to free the `restoration` stem for the forthcoming object-conservation engine, whose home meaning it is. The REFERENCES keeps "restoration orientation" as the Stroebe & Schut theory term. Renaming a member is a breaking change (composites hard-link members by name), so this is a minor bump, not a patch.
