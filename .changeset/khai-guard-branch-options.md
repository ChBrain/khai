---
"@chbrain/khai-guard": patch
---

Add `--staged-only` / `-s` and `--tracked-only` / `-t` options to the `khai-guard branch` CLI command to allow selective change resolution during branch creation when untracked files are present.
