---
---

Add the reclamation engine's tests: package conformance (content + manifest + compose), the manifest shape (an intervention root over four stances, wired on place at Shown), and compose() carrying the reclamation root above each stance. Tests only; no shipped content changes.
