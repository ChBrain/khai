---
"@chbrain/khai-engine-psychological-safety": minor
---

Take the engine's own name on the two expressions that collided:
`place_high` → `place_high_safety`, `place_low` → `place_low_safety`. A bare
`high` or `low` names no phenomenon, and three engines were holding each.
Member files are API and renaming one is breaking, hence minor.
