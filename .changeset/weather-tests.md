---
---

Add the weather engine's tests (packages/engines/weather/tests/weather.test.mjs), the dormant tests PR that follows the merged source per the source-and-tests-separate rule. Validates conformance (validateEnginePackage with executeCompose), the manifest (process root over six channels and their bands, the four/three/two-band ladders, both wiring altitudes including the first-of-kind place/Shown link), and compose() (every leaf composes root-first carrying the weather root; a band carries its channel between the root and itself; unknown and missing leaves throw). Tests only; no package content or version change.
