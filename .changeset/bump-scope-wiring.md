---
---

CI job + pre-push hook + unit tests for the bump-scope gate. Test and tooling
only, no package source change, so no release (the gate's source rides its own
`@chbrain/khai-guard` patch in the source PR).
