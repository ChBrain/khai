---
---

Close the ratchet on the two stems the journey-roles rename freed: delete
`shadow` and `trickster` from `memberPolicy.homonyms`, taking the list from 31
to 29, and mark the two backlog rows shipped (#1369). Documentation only.
