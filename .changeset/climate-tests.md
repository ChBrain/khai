---
---

Add the climate engine's tests: package conformance (content + manifest + compose), the manifest shape (a regime root over four modes, wired on place at Shown), and compose() carrying the climate root above each mode. Tests only; no shipped content changes.
