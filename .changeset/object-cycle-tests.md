---
---

Add the object-cycle composite's tests: package conformance (content + manifest + compose, hard links resolving through the declared atoms), the manifest shape (a life root over three bridges, wired on piece at Apparent with the cargo advisory/audit), the three re-exported atoms (wear, decay, restoration), and compose() carrying the object-cycle root above each bridge. Tests only; no shipped content changes.
