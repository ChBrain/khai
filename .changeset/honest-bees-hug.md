---
---

Add the `hoarding` composite: hoarding read as a pile of decisions not made
rather than of things kept, over six atoms. First release — the manifest
version is the initial version, so this changeset is empty by rule 4.
