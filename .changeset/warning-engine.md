---
---

Add the warning engine: a harm signalled before it arrives -- the issuing, the receiving, the repetition, and the aftermath -- carrying the twist that a warning that works looks exactly like one that was wrong. First release, so the manifest version is the initial version and the changeset is empty.
