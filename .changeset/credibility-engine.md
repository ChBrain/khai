---
---

Add the credibility engine: the credit extended to a first-person account -- the report, the discount, the missing word, and the aftermath -- carrying the twist that being disbelieved leaves a persona holding less of a truth rather than an unheard one. First release, so the manifest version is the initial version and the changeset is empty.
