---
---

Close the ratchet on the two stems the negotiation rename freed: delete
`exploration` and `resolution` from `memberPolicy.homonyms`, taking the list
from 29 to 27, and mark the two backlog rows shipped (#1368). Records the
`shadow`/`trickster` decision and the state of the backlog now that the
multi-entry lanes are done. Documentation only.
