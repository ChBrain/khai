---
---

Add the hearth engine tests: the canon conformance sweep (validateEnginePackage with
executeCompose), the manifest shape (a process root over five modes on place/Shown),
and the compose chains, following the standard content-package test shape. Lands after
the engine per the source/tests rule.
