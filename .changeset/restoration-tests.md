---
---

Add the restoration engine's tests: package conformance (content + manifest + compose), the manifest shape (a mending root over five stances, wired on piece at Apparent), and compose() carrying the restoration root above each stance. Tests only; no shipped content changes.
