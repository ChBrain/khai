---
---

Add the photograph engine: an image that certifies a moment occurred and cannot say what it was -- the pictured, the taking, the showing, the outliving -- carrying the twist that a photograph is taken to keep a moment and is the reason the moment is not kept. A piece engine, so the members hang flat beneath the object. First release, so the manifest version is the initial version and the changeset is empty.
