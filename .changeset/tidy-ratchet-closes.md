---
---

Close the ratchet on the three stems the folktale rename freed: delete
`preparation`, `recognition` and `return` from `memberPolicy.homonyms`, so the
member-scope gate hard-blocks any re-entry rather than waving it through.
Sequenced behind the rename that made them dead. Documentation only.
