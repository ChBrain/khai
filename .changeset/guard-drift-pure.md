---
"@chbrain/khai-guard": patch
---

Export the drift comparison as `checkDrift` from the package index, matching every other check. The bin asks the registry and prints; the index decides. Behaviour is unchanged.
