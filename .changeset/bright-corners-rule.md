---
---

Cover the `adhd` composite: canon conformance through the shared kit, the
manifest contract including the four movements as a set, the atoms matching the
declared dependencies, and compose() over every leaf. Tests only; they do not
ship.
