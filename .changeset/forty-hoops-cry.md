---
---

Add the `borderline` composite: borderline organisation read as Linehan's
biosocial transaction across four movements, with the boundary against `cptsd`
declared. First release — the manifest version is the initial version, so this
changeset is empty by rule 4.
