---
---

Add the succession engine's tests: package conformance (content + manifest + compose), the manifest shape (a reclaiming root over four modes, wired on place at Shown), and compose() carrying the succession root above each mode. Tests only; no shipped content changes.
