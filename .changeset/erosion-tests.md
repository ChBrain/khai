---
---

Add the erosion engine's tests: package conformance (content + manifest + compose), the manifest shape (a wearing root over four modes, wired on place at Shown), and compose() carrying the erosion root above each mode. Tests only; no shipped content changes.
