---
"@chbrain/khai-engine-power": minor
---

Take French & Raven's own term on the one base that collided:
`position_reward` → `position_reward_power`, freeing `reward` for the engine
that owns it. The file's title already read "Reward Power". Member files are API
and renaming one is breaking, hence minor; nothing outside the engine linked it.
