---
---

Complete the Tier F type coverage in docs/ENGINE-GAPS.md: pitch and performance, at charting-research depth. Enumerate types from architecture/ (not instance frontmatter, which missed performance).

F-pitch: separate the two questions the first pass collapsed. Wiring is a settled no (pitch is structurally outside the requires.on graph). But content is open and large: pitch has 4 instances, all from palimpsest, all in Genette's hypertextual sub-genus; the whole genus of affective/modal keys (Frye's mythoi, Fowler's mode) is unbuilt and buildable as new pitch_* engines on play/Arc. Charts a candidate table (elegiac, melodramatic, pastoral, absurdist, epic/heroic, grotesque high-to-medium; ironic/noir/gothic/sentimental maintainer-call), the hard rejects (pitch_tragic/comic/uncanny/sublime -- same phenomenon owned at persona level, member-check would reject), and off-type tonal forces (bathos as the cleanest new build; within-plot modulation on plot/Cue as a loose thread).

F-performance: reframe from BLOCKED to a bounded born-the-type seam. Corrects the result-layer model -- khai-writing is a separate repo where plays' performances are stored, not a package to mint inside this monorepo (no writing/* lane, no packages/khai-writing). Splits the seam: the type-birth half is ours (template_performance.md, a validate on:performance check, a first instance, status draft->published); the result layer is external; the one open question is cross-repo Lodged resolution, a maintainer-scope call. The 7 forces unblock together once the type is born.

Planning-doc bookkeeping only; no package change.
