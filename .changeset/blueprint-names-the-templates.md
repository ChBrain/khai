---
"@chbrain/khai-stage": patch
---

The blueprint now tells a house where a file starts.

Every house khai-stage raises already devDepends on `@chbrain/khai-arch`, so the
nine authoring templates are installed on day one, complete and valid. Nothing
in the blueprint named them: not CLAUDE.md, not GEMINI.md, not the Copilot
instructions, not the management instructions, and not the Playwright's own
position, whose Orders say to translate stories into the ENACTS structure and
were silent on what to start from.

CLAUDE.md gains a "Starting a file" section ahead of Branching, because writing
precedes choosing a lane. The Copilot file gets a pointer at it rather than a
second copy. The Playwright's Orders gain one clause.

It also says the part a scaffold makes easy to miss: a stamped template
validates, which is what makes it a safe start and what makes an unedited one
shippable.
