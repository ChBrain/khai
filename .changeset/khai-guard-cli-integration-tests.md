---
---

Add bin-level integration tests for the khai-guard CLI: three-dot diff range
(the 0.0.3 regression), source/test mix, pure-rename exemption, bucket overlap,
missing flag value, and local-mode skip. Test-only — no published artifacts
change, so this carries an empty changeset (no version bump).
