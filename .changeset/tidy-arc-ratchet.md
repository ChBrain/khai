---
---

Close the ratchet on the three stems the narcissism rename freed: delete
`admiration`, `communal` and `rivalry` from `memberPolicy.homonyms`, taking the
list from 34 to 31, and mark the three backlog rows shipped (#1366).
Documentation only.
