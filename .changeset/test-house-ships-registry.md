---
---

Stage test now asserts the raised house ships (`files`) and exports
(`exports["./registry.json"]`) its `registry.json` — locking the regression
fixed in the template (no package behaviour change).
