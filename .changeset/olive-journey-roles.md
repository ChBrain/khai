---
"@chbrain/khai-engine-journey-roles": minor
---

Name the two Vogler roles as roles: `persona_shadow` → `persona_shadow_role`,
`persona_trickster` → `persona_trickster_role`, matching `persona_journey_role`
beside them and freeing `shadow` and `trickster` for the `archetypes` engine
that holds Jung's. Member files are API and renaming one is breaking, hence
minor; nothing outside the engine linked either.
