---
"@chbrain/khai-tests": patch
---

Add an Atoms column to the science index's By engine table: which engines each composite wires, read from its own dependencies. Resolves a composite that wires composites, not only engines.
