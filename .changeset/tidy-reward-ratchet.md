---
---

Close the ratchet on the stem the power rename freed: delete `reward` from
`memberPolicy.homonyms`, taking the list from 43 to 42, and mark the backlog row
shipped (#1358). Sequenced behind the rename that made it dead. Documentation
only.
