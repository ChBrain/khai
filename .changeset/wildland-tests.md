---
---

Add the wildland composite's tests: package conformance (content + manifest + compose, hard links resolving through the five declared atoms), the manifest shape (a land-life root over three bridges), the wiring (the law at fail and a single place/Shown cargo, advisory/audit), the five re-exported atoms (geomorphology, erosion, hydrology, fire, succession), the assertion that climate is the referenced frame and not a wired atom, and compose() carrying the wildland root above each bridge. Tests only; no shipped content changes.
