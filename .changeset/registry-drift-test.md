---
---

Test-only: add coverage for the registry build-drift gate (a fresh build passes; a hand-edited description, reordered entries, or a stale entry each fail). Ships no package content: an empty changeset.
