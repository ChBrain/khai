---
---

Whitelist `gratitude` under `memberPolicy.homonyms`, mirroring `pride` and `envy`: a word that legitimately lives at two altitudes -- the dispositional character pole under the `virtue` engine (`position_gratitude.md`, the axis-partner of `envy`) and the standalone emotion engine it deserves in its own right. This clears the member-scope gate for a forthcoming `gratitude` engine, exactly as the same entry already clears it for `pride` and `envy`. Governance config only; ships nothing. The ratchet's dead-exemption warning is expected until the `gratitude` engine lands and makes the collision live.
