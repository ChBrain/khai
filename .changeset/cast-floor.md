---
"@chbrain/khai-tests": patch
---

Wall (order 1, cut-to-fit): gate the cast floor. A play that fields personas fields at least three (a debate needs three voices); the floor is conditional, so a persona-less structural play (an engine's folktale, monomyth, or dramatic arc) is exempt. Runs per play over the Company, alongside the casting-coverage check. Verified green across all four current plays (chain_change_landing fields three; the three engine plays field none and are exempt). The order's companion floor, "at least one plot per beat", is deferred: the canon has no "beat" concept to count against, so gating it would be judged, not computed, and it returns to the wall once the term is defined. Set at patch as the free level; a new gate may warrant a minor at the maintainer's `bump:minor` label.
