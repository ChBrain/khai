---
---

Add the captology engine's tests: package conformance (content + manifest + compose), the manifest shape (a hook root over four behavioral facets, wired on piece at Load Bearing), and compose() carrying the root above each facet. Tests only; no shipped content changes.
