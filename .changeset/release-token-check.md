---
---

Adds `scripts/release-token.mjs` and `.github/workflows/release-token.yml`, a monthly report of whether RELEASE_TOKEN still authenticates and still reaches this repository and every house on the bill; ships nothing, so this is an empty changeset.
