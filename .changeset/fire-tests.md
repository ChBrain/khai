---
---

Add the fire engine's tests: package conformance (content + manifest + compose), the manifest shape (a burn root over four modes, wired on place at Shown), and compose() carrying the fire root above each mode. Tests only; no shipped content changes.
