---
---

Add the usability engine's tests (packages/engines/usability/tests/usability.test.mjs), the tests PR that follows the merged source per the source-and-tests-separate rule. Validates conformance (validateEnginePackage with executeCompose), the manifest (piece root over four design facets, both wiring altitudes including the first-of-kind piece/Apparent link), and compose() (every leaf composes root-first carrying the usability root; the root sits above each facet; unknown and missing leaves throw). Tests only; no package content or version change.
