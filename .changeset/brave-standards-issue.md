---
---

Open the `disability` engine: the standing a persona is put in by arrangements
built for a different body — the specification nobody wrote down, the encounter
where the mismatch appears, the adjustment that must be asked for, and the access
added afterwards. First release, so the manifest version is the initial version
and this changeset carries no bump.
