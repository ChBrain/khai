---
---

Convert `memberPolicy.homonyms` to the map shape, carrying the distinct stem the
chase list already chose for each of the 27 live entries, so `member-check` can
name the replacement at the moment a change touches the engine holding it.
Records the ratchet in `CLAUDE.md` rule 7. Config and documentation only.
