---
---

Add the time composite's tests: package conformance (content + manifest + compose, hard links resolving through the declared atoms), the manifest shape (a whole-time root over three bridges), the multi-cargo wiring (the law at fail, every cargo advisory/audit, and exactly the four family cargoes -- piece/Yearbook, plot/Cue, plan/Implementation, process/Initiated by), the two re-exported atoms (chronos, kairos), and compose() carrying the time root above each bridge. Tests only; no shipped content changes.
