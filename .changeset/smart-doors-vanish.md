---
---

Add `npm run gates`: every wall in one command, one exit code, and a summary
built to be pasted. Widen the source/test globs to the content packages the
rule's own text already named, and state the two enforced content rules no
document carried. Tooling and prose; no package changes.
