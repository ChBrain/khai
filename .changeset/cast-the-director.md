---
"@chbrain/khai-plays": patch
---

Cast the Director in the chain's reference company: `position_director` (the
hand that stages a score into a performance), `persona_director` (Thespis, the
reference Director; each house casts its own), and `plan_stage_the_score` (the
Director's render mandate). Completes the blueprint cast Order 1 builds the
skill against.
