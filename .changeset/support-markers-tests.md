---
---

Cover the support vocabulary: eight tests, dormant until the source lands.

Two blocks. `roleOf` reading a declared `supportingMarkers` list mid-cell, falling
back to a default as contrast does, and **keeping prose a spine** -- the failure
that would make the vocabulary useless is a cell mentioning the idea in passing
being read as a claim about it. And the one that matters: **a marker-declared
background clears the shared-work wall, from either side.** That is the whole
point of bringing the vocabulary home, since a house's own list used to exempt
its own instrument and not the wall.

One test asserts declaring a list REPLACES the default rather than extending it,
the same contract `contrastMarkers` has, so a house that narrows its vocabulary
gets the narrowing it asked for.

The sentinel probes for the fix itself rather than a sentence: a sentence in a
wrapped comment is not a contiguous string, and a sentinel that never matches
leaves the block quietly skipped -- green, and testing nothing. Tests only.
