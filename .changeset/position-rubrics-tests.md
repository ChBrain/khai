---
---

Tests for the position-derived rubric resolver: `buildPositionInstruction` (carries the accountability, names one voice or several held in tension, frames the rubric as an escalation rung that defers to the gates and never gates, includes the shadow to distrust when present) and `resolvePositionRubrics` (one rubric per position with the count the house's own, id from the filename, a holding persona's voice and shadow folded in, two personas held in tension, a position with no Drives skipped, stable filename order, and a graceful empty for a missing directory). Tests only; ships nothing.
