---
---

Add the artifice composite's tests: package conformance (content + manifest + compose + hard links resolving through the five declared engine dependencies), the manifest shape (a root over three bridges, wired on piece at Load Bearing at audit level), that all five design engines re-export as atoms, and compose() carrying the artifice root above each bridge. Tests only; no shipped content changes.
