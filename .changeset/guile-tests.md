---
---

Add the guile engine's tests (packages/engines/guile/tests/guile.test.mjs), the tests PR following the merged source per the source-and-tests-separate rule. Validates conformance (validateEnginePackage with executeCompose), the manifest (piece deceit root over five dark-pattern facets, both wiring altitudes with the piece link at Apparent), and compose() (root-first, root above each facet, unknown/missing leaves throw). Tests only; no package content or version change.
