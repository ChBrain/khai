---
---

Add the bell engine: the instrument that reaches everybody at once -- cast in an afternoon and issued for four hundred years, rung as a broadcast with no way to switch it off, and defining a community by where the sound happens to carry. First release, so the manifest version ships as-is.
