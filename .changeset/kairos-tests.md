---
---

Add the kairos engine's tests: package conformance (content + manifest + compose), the manifest shape (a moment root over four facets), the multi-cargo wiring (the law plus the moment-chapter of each unfolding type -- plot/Cue, plan/Implementation, process/Initiated by -- and exactly those three cargoes), and compose() carrying the kairos root above each facet. Tests only; no shipped content changes.
