---
---

Whitelist `disgust` and `sensitivity` under `memberPolicy.homonyms`, so the forthcoming standalone **disgust** engine can carry its root and its trait member without colliding. `disgust` is the promotion case (mirroring `narcissism`): the `emotion` engine keeps disgust as one node of the basic-emotion appraisal taxonomy (`process_disgust.md`), while the standalone disgust engine unfolds its internal structure -- the three functional domains (pathogen/sexual/sociomoral), the contamination mechanism, and the disgust-sensitivity trait. `sensitivity` is a same-word-different-science homonym: caregiver sensitivity in the `caregiving` engine (`process_sensitivity.md`, Ainsworth's responsiveness to a care-target's signals) versus disgust sensitivity in the disgust engine (the standing trait, the Disgust Scale). These clear the member-scope gate for the disgust engine's `process_disgust.md` and `position_sensitivity.md`. Governance config only; ships nothing. The ratchet's dead-exemption warning is expected on each stem until the disgust engine lands and makes the collisions live.
