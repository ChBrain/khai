---
---

Close the ratchet on the three stems the bias rename freed: delete `anchoring`,
`reactance` and `representativeness` from `memberPolicy.homonyms`, taking the
list from 42 to 39, and mark the three backlog rows shipped (#1360). Sequenced
behind the rename that made them dead. Documentation only.
