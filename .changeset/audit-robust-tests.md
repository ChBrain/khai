---
---

Tests for the audit CLI harness upgrade: `reviewCard`/`reviewMarkdown` confirm a flag on consensus when given thresholds, drop a finding below the threshold, stay single-shot when no thresholds are given (back-compat), and run robust over H2 sections; and a CLI spawn test where a manifest's `fromPositions` resolves the house's rubrics from its management positions, runs to an advisory exit 0, and logs a finding tagged to the resolved rubric. Tests only; ships nothing.
