---
---

Pin the non-author Source declaration as a vocabulary rather than a list, dormant until the source lands. Tests only: `packages/khai-tests/tests/non-author-sources.test.mjs` skips while `matchesNonAuthor` does not exist, so this PR ships no package change and takes an empty changeset.
