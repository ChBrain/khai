---
---

Add the lottery engine: a good allocated deliberately at random -- the impasse, the drawing, the verdict, and the filling -- carrying the twist that a lottery works by admitting no reasons, which is why everybody it touches invents one. First release, so the manifest version is the initial version and the changeset is empty.
