---
"@chbrain/khai-arch": patch
---

Introduce `plan` type (TO DOIT) spec, templates, and card extraction, exporting `planCard` and `planChapters`. Kept `orderCard` and `orderChapters` as deprecated aliases for backwards compatibility.
