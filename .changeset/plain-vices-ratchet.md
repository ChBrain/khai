---
---

Close the ratchet on the three stems the virtue rename freed: delete `envy`,
`gratitude` and `pride` from `memberPolicy.homonyms`, taking the list from 46 to
43, and mark the three backlog rows shipped (#1355). Sequenced behind the rename
that made them dead. Documentation only.
