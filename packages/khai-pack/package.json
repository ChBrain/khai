{
  "name": "@chbrain/khai-pack",
  "version": "0.0.2",
  "description": "khai-pack: the serve engine. Turns a typed bundle spec into a deterministic, guarded zip in the khai 'cultures' layout (overhead at root, flat content in a subfolder). Kind-agnostic: consumers register what their bundle carries and how it is checked.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chbrain/khai.git",
    "directory": "packages/khai-pack"
  },
  "license": "MIT",
  "type": "module",
  "main": "./lib/index.mjs",
  "exports": {
    ".": "./lib/index.mjs",
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "files": [
    "lib/",
    "README.md"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
