{
  "name": "@chbrain/khai-rules",
  "version": "0.1.11",
  "description": "khai rule atoms — the pure, canon-agnostic validation mechanism. Each checker takes its contract as an argument and knows nothing about which types exist.",
  "type": "module",
  "main": "./index.mjs",
  "exports": {
    ".": "./index.mjs",
    "./package.json": "./package.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chbrain/khai.git",
    "directory": "packages/khai-rules"
  },
  "license": "SEE LICENSE IN LICENSE and LICENSE-CODE",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "files": [
    "index.mjs",
    "rules.mjs",
    "parse.mjs",
    "README.md"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "js-yaml": "^5.2.1"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
