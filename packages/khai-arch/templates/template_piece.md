---
khai: piece
title: "[title]"
license: CC-BY-NC-SA-4.0
stamp:
  owner: KAI HACKS AI
  version: v0.0.1
  date: "2026-06-03"
---

# Piece: [title]

## Taxonomy

The group above this piece: the set or kit it is one of. Name it, then ask whether that set already has its own file. If it does, link it; if it does not, ask whether it should. This is the collection this piece belongs to, not the piece's own name (the H1 carries that) and not where it came from (Owner carries that).

## Owner

- Project: [project]

## Place

Where the piece sits in the world: one spot, or many if it recurs (a founder's portrait in every dojo). A piece is always somewhere, so if you cannot say where, it is not placed yet. Link each where it already has a file; where you cannot, ask whether it should use a khai type; if it travels with a persona, write where that persona is (this one does not link, and that is correct).

## Load Bearing

What collapses if this piece is removed. Remove it: if only its absence shows and nothing the world claims about itself falls into contradiction, it is set dressing, not load-bearing. Link what would collapse where that has a file; where it does not, ask whether it should use a khai type.

## Apparent

What the piece looks like: physical, present tense, concrete. If it states what the piece means rather than what is plainly there, that belongs in Load Bearing, not here. Where a visible part carries its own weight, link it if it already has a file, or ask whether the piece should be split and that part become its own piece.

## Yearbook

The accumulated history: everywhere it has been and everything it has done, told as a story in time. The marks need not be absolute dates; relative ones work (morning, after lunch, at night). A piece without history is not yet a piece: if you cannot place a single entry in time, it is just an object. Link what it has touched where that has a file; where it does not, ask whether it should use a khai type.
