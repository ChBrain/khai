# @chbrain/khai-engine-reversal

## 0.1.1

### Patch Changes

- eb77080: Add reversal engine: the inversion of a situation's direction, with approach, turn, and aftermath phases.
- 9574e59: Declare the `@chbrain/khai-arch` runtime dependency that `index.mjs` imports (`compositionOrder`); it previously resolved only via workspace hoisting, so a standalone install failed to load. Also declare the `@chbrain/khai-tests` devDependency the suite imports and align vitest to `^4.1.9`.

## 0.1.0

### Patch Changes

- Add reversal engine: the inversion of a situation's direction, with approach, turn, and aftermath phases.
