{
  "name": "@chbrain/khai-engine-gender",
  "version": "0.0.4",
  "description": "khai engine content — the gender domain: gender as position, the read the room applies before a persona speaks.",
  "type": "module",
  "main": "./index.mjs",
  "exports": {
    ".": "./index.mjs",
    "./package.json": "./package.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chbrain/khai.git",
    "directory": "packages/engines/gender"
  },
  "license": "CC-BY-NC-4.0",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "files": [
    "index.mjs",
    "*.md"
  ],
  "khai": {
    "engine": "gender",
    "type": "position",
    "anchor": "position_gender.md",
    "expressions": {
      "male": "position_male.md",
      "female": "position_female.md"
    },
    "requires": [
      {
        "on": "persona",
        "section": "Projection",
        "link": "expression"
      }
    ],
    "wiring": "Always-on positional read, wired at two altitudes. Declare the law once in the instructions Knowledge chapter (\"personas have a gender\", linking the anchor) so the read applies system-wide and cannot be dropped; then give each persona exactly one expression under Projection, which carries the anchor upward so the anchor need not be linked separately. The law mandates the anchor for all; each Projection resolves it to a concrete expression. Read in the first beat of the scene and held continuously underneath every other process the persona runs — it does not pause. The agent never narrates the position; the room enacts it, in who gets the first word and who is read as a claim before they speak.",
    "requirement": "Every persona must link one gender expression under its Projection section. The engine declares this; @chbrain/khai-tests enforces it on any project that installs the engine."
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@chbrain/khai-arch": "^0.0.6"
  },
  "devDependencies": {
    "@chbrain/khai-tests": "^0.0.4",
    "vitest": "^4.1.7"
  }
}
