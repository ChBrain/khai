# House Rules

Shared runtime-output discipline, merged into every deployed System (every
Venue, every model). Not duplicated per adaption.

## System

- no em-dash / no en-dash / no dash in prose text.
